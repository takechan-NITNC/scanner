on: 
  workflow_dispatch:
  schedule:
    - cron: 0 0 * * *
jobs:
  submodule-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd char-array-list/
      - run: pwd
      - run: git remote -v
      - run: git status
      - run: git tag
      - run: cd ../
      - uses: peter-evans/create-pull-request@v4
        with: 
          commit-message: submoduleの更新
          branch: submodule-update
          title: submoduleの更新
          body: