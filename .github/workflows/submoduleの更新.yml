on: 
  workflow_dispatch:
  schedule:
    - cron: 0 0 * * *
jobs:
  submodule-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd char-array-list/ && git tag
      - uses: peter-evans/create-pull-request@v4
        with: 
          commit-message: submoduleの更新
          branch: submodule-update
          title: submoduleの更新
          body: